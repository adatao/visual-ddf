// import vDDF from './vddf';

// we can't test it yet because adaviz got compile error when running with babel :(
describe('vDDF', () => {
  it('should return modified status correctly');

  it('should automatically detect schema type when schema or data is updated');

  it('should not modified when reassign same schema');
});
